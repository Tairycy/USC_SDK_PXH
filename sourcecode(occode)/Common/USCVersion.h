#ifndef usc_USCVersion_h
#define usc_USCVersion_h

static NSString *sdk_version = @"2.0.14"; // 新接口
//static NSString *sdk_version = @"3.0";// http 协议同步该异步
//static NSString *sdk_version = @"1.6"; // 使用http协议，语音语义发送同一个请求
//static NSString *sdk_version = @"1.6.1";// 可以设置语言，可以远近讲,，设置了tts的手机信息参数，修复返回的数据不能播放问题，替换成了最新的vad静态库支持在iOS6上运行。添加了直接识别音频文件的接口，添加了返回tts合成的语音数据的接口。修改reachablity 中会冲突的变量。
#endif
